<t:container xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd"
	xmlns:p="tapestry:parameter">

	<t:if test="loss">
		<span class="lossformat">${value}</span>
		<p:else>
			<span class="profitformat">${value}</span>
		</p:else>
	</t:if>
</t:container>